import { storyActions } from '../constants/action-types';

export const setConversation = (conversationSequence: string) => ({
  type: storyActions.SET_CONVERSATION,
  payload: {
    conversationSequence
  }
});
