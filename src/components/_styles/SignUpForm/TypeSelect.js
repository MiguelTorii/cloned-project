export const styles = (theme) => ({
  root: {
    display: 'flex',
    width: '100%',
    alignItems: 'center',
    justifyContent: 'space-around',
    margin: theme.spacing(2)
  },
  hide: {
    display: 'none'
  }
});
