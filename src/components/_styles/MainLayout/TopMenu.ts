import { makeStyles } from "@material-ui/core/styles";
export default makeStyles(theme => ({
  menuPaper: {
    top: `${theme.spacing(8.5)}px !important`
  },
  avatarMenu: {
    padding: theme.spacing(2, 0)
  }
}));