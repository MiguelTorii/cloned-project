import makeStyles from '@material-ui/core/styles/makeStyles';

export default makeStyles((theme: any) => ({
  container: {},
  feedback: {
    cursor: 'pointer'
  },
  emptyContent: {
    maxWidth: 570,
    textAlign: 'center'
  }
}));
